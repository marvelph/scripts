#!/bin/bash
set -euo pipefail

profile=$(aws configure list-profiles | fzf --preview 'awk '\''( "{}" == "default" && $1 == "[default]" ) || ( $1 == "[profile" && $2 == "{}]" ) { found = 1 ; print $0 ; next } found && $1 ~ /^\[.+$/ { exit } found { print $0 }'\'' ~/.aws/config' --header="Select an AWS profile" --footer="aws sso login --profile <selected>")
aws sso login --profile "$profile"

